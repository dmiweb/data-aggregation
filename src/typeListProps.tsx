export type MonthListProps = {
  list: {
    month?: string,
    amount: number
  }[]
}

export type YearListProps = {
  list: {
    year?: number,
    amount: number
  }[]
}

export type SortDateListProps = {
  list: {
    date?: string,
    amount: number
  }[]
}